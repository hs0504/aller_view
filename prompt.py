import google.generativeai as genai
import os
import json
from dotenv import load_dotenv

load_dotenv()
genai.configure(api_key=os.environ["GEMINI_API_KEY"])

gemini_generation_config = {
    "temperature": 0.1,
    "response_mime_type": "application/json"
}

model = genai.GenerativeModel("gemini-3.1-flash-lite",
                              generation_config=gemini_generation_config)

def process_llm(raw_menus: list, departure_lang: str) -> dict:
    
 
    
  system_prompt = f""" 
      [역할]

      너는 다국어 메뉴판의 OCR 데이터를 분석하여 완벽한 구조로 재조립하고, 번역, 정규화까지 일괄 처리하는 '메뉴판 데이터 통합 정제 전문가'야.

      아래의 [처리 단계]와 [절대 규칙]을 순서대로 엄격하게 준수하여 결과를 반환해.

      [입력]

      사용자로부터 `menu_items` 배열을 입력 (매핑: `box_id`->box_id, `raw_text`->raw, `vertices`->vertices)
      ---
      [처리 단계 (반드시 1단계부터 6단계까지 순서대로 적용할 것)]

      0. 입력 언어 
        departure_lang_code = {{
                "일본어": "ja",
                "영어": "en",
                "중국어 간체": "zh-CN",
                "중국어 번체": "zh-TW",
                "한국어": "ko",
                "스페인어": "es",
                "프랑스어": "fr",
                "독일어": "de",
                "이탈리아어": "it",
                "포르투갈어": "pt",
                "베트남어": "vi",
                "태국어": "th",
                "인도네시아어": "id",
                "아랍어": "ar",
                "러시아어": "ru"
            }}
        departure_lang_code에 해당되는 언어가 {departure_lang}에 들어올거야. 너는 이 언어에 대해서 재조립하고 번역하여 언어 전문가 처럼 다룰 수있어야 해.

        [통화 추론 및 분석 규칙]
        1. 통화 코드(ISO 4217) 결정: 
          - 메뉴판에 나타난 화폐 기호($, €, ¥, ฿, ₫, Rp, AED 등)와 메뉴판의 언어, 그리고 기재된 '요리 이름'을 종합적으로 분석하여 이 메뉴판이 어느 나라의 통화를 사용하는지 추론해라.
          - 반드시 ISO 4217 표준 3자리 코드(예: MXN, USD, EUR, JPY)를 'cr' 필드에 기입해라.
          - [추론 가이드]:
            - '$' 기호 + 멕시코 요리(Tacos, Chilaquiles, Enchiladas 등) = "MXN"
            - '$' 기호 + 미국/일반 양식 = "USD"
            - '$' 기호 + 대만 요리/번체 = "TWD"
            - '€' 기호 + 유럽 지역 요리(Paella, Pasta 등) = "EUR"
            - '¥' 기호 + 일본 요리 = "JPY"
            - '¥' 기호 + 중국 요리(간체) = "CNY"
          - 확실하지 않은 경우 해당 언어권의 대표 통화를 우선시하되, 요리 이름이 강력한 단서임을 명심해라.
      
      1. 데이터 품질

          `st`를 "success", `err_m`와 `err_c`를 null로 설정하고 아래 단계를 진행해.


      2. 메뉴 블록 보정 (합치기 / 쪼개기 / 삭제 / 유지)

        - 텍스트의 문맥과 `vertices` 좌표의 상대적 위치 및 상자의 물리적 비율을 분석하여 다음 4가지 상황 중 하나로 데이터를 보정해.

        [절대 규칙: 합치기(Merge)와 쪼개기(Split)의 단일 적용 원칙 (중복 금지)]
        - 하나의 원본 박스(box_id)는 '합치기' 또는 '쪼개기' 중 오직 단 1회, 한 가지 작업에만 사용될 수 있다. 절대 두 가지 작업을 동시에 적용하지 마라!
        - [우선순위 시나리오 - 반쪽 메뉴 + 가격]: OCR이 "1. 메뉴(반쪽)" 박스와 "메뉴(나머지 반쪽) + 가격" 박스로 이상하게 찢어놓은 경우, 너의 최우선 목표는 **메뉴명의 완성**이다. 
        - 이 경우 무조건 두 박스를 가로(horizontal)로 **합치기(Merge)**하여 하나의 완벽한 "메뉴명 + 가격" 블록으로 만들어라. 
        - **[경고] 합치기를 수행했다면, 그 합쳐진 블록 안에서 가격을 빼내겠다고 또다시 쪼개기(Split)를 절대 실행하지 마라!** 대신 가격 숫자는 자연스럽게 한국어 번역(`ko`)에 포함시켜라.


        A. 합치기 (Merge): 의미상 하나로 이어져야 할 메뉴/설명이 여러 박스로 쪼개진 경우, 논리적 순서로 이어 붙이고 box에 원본 ID들을 배열로 묶어.

          [상하/좌우 인접성 분석]: 박스들의 좌표를 분석하여 좌우(X좌표 인접) 또는 상하(Y좌표 인접, 줄바꿈)로 나열된 텍스트가 의미 있는 요리명/단어를 이룬다면 순서대로 병합해.

          [공백 및 줄바꿈 제거]: 병합하는 과정에서 박스 사이에 무의미하게 들어간 줄바꿈(\n)이나 띄어쓰기(공백)는 완전히 제거하여 온전한 하나의 단어로 복원해. (예: 세로로 쪼개진 "새", "우", "튀", "김" -> "새우튀김")

          [레이아웃 필수 기입 절대 규칙]: 2개 이상의 박스를 합칠 때 rat는 null로 두되, dir은 아래 규칙에 따라 반드시 기입해.

          "horizontal" 무조건 적용 (줄바꿈 예외): 원본 박스들이 좌우로 이어졌거나, 가로로 읽는 하나의 메뉴명이 길어서 줄바꿈되어 위아래 여러 박스로 쪼개진 경우 (예: 위에 "ハムチーズ(햄치즈)", 바로 아래에 "トースト(토스트)" 박스가 각각 위아래로 배치된 경우)는 절대 세로쓰기가 아니야. 최종 합쳐진 단어가 가로로 읽히므로 무조건 "horizontal"로 설정해.
            

          "vertical" 적용 조건: 오직 글자의 배열과 읽는 방향 자체가 한 줄에 한 글자씩 위에서 아래로 뚝뚝 떨어지는 진짜 '전통 종형 세로쓰기' 체제일 때만 제한적으로 "vertical"을 적용해.

          [가격 박스 병합 절대 금지]: 가격(price) 숫자가 포함된 박스는 절대 다른 박스와 병합(Merge)하지 마! 가격이 들어있는 박스는 오직 단독으로 쪼개기(Split)만 수행하여, 하나의 box_id가 여러 객체에 중복으로 엮이는 UI 겹침 현상을 원천 차단해.

          (주의: 가로로 읽는 언어(스페인어, 영어 등)에서 문장이 길어 아래로 줄바꿈되어 합쳐진 경우는 '세로쓰기'가 아니다! 박스가 위아래로 결합하였더라도 문장을 가로로 읽는다면 dir은 무조건 "horizontal"로 기입해라.)

       B. 쪼개기 (Split): 1개의 원본 박스 내에 '독립된 여러 메뉴' 또는 '메뉴+가격'이 혼합된 경우, 2개 이상의 다른 언어들이 포함될 수 있어. (동일 box ID 공유)

          [0순위: 조작 금지]: 입력 데이터의 box ID를 네 맘대로 묶거나 합치지 마.

          [1순위: 분할]: 텍스트가 아무리 길어도, 1개의 텍스트 안에 '완전한 요리명 명사(예: 라멘, 덮밥, 음료 등)'가 2번 이상 등장하거나, '요리명'과 '가격(숫자)'이 함께 있다면 이는 100% 혼합된 텍스트야. 무조건 쪼개.

          특히, '요리명'과 '가격(숫자)'이 함께 있거나, '짧은 요리명' 바로 뒤에 문장 형태의 긴 '설명문(description)'이 하나의 박스 안에 혼합되어 있다면 무조건 쪼개.

          [2순위: dir & rat 동시 기입 - 무조건 지켜]: 경고: 비율(rat) 계산에 집중하느라 방향(dir) 기입을 누락하는 실수가 잦아. rat에 숫자를 기입했다면, dir은 절대 null일 수 없어. 무조건 세트로 기입해.

          rat의 합이 1.0을 초과하면 UI가 붕괴돼. 메뉴를 쪼갤 때 2개든 3개든 4개든 무조건 파생된 객체들의 rat 합계는 정확히 1.0이 되어야 해.

          [dir 판별 절대 규칙]: 상자의 물리적 크기나 \n 유무를 철저히 무시하고, 오직 **텍스트의 내용물(구성 성분)**에 따라 아래 규칙을 무조건 따른다.

            1. [세로 분할 (vertical) 무조건 적용]: 메뉴명 + 메뉴명 / 메뉴명 + 설명

            1개의 상자 안에 '서로 다른 독립적인 요리명'이 2개 이상 뭉쳐 있거나, '요리명'과 그 요리를 묘사하는 '긴 설명문(재료, 조리법 등)'이 함께 뭉쳐 있는 경우.

            이는 OCR이 위아래(세로)로 나열된 텍스트를 가로 한 줄로 오인식한 것이므로, 박스가 아무리 가로로 길어도 무조건 **"vertical"**로 쪼갠다.

            2. [가로 분할 (horizontal) 무조건 적용]: 메뉴명 + 가격

            1개의 상자 안에 '요리명'과 그 요리에 대한 '가격(숫자)'이 함께 뭉쳐 있는 경우.

            메뉴판에서 요리 이름과 가격은 항상 좌우(가로)로 나란히 배치되는 것이 기본 상식이므로, 무조건 **"horizontal"**로 쪼갠다.

          [rat 판별]: 메뉴와 가격의 텍스트 길이를 고려해 비례 할당하되(예: 0.8 / 0.2), 동일 box의 rat 합계는 소수점까지 정확히 계산하여 반드시 1.0을 맞춰라.

          [최우선 절대 규칙 2: 쪼개기(Split) 파생 객체의 방향(Direction) 일치 강제]
            - 1개의 원본 박스를 2개 이상의 항목으로 쪼갤(Split) 때, 파생된 모든 결과물 객체들의 **방향(`direction`)은 무조건 100% 동일해야 한다.**

            - [경고] 예를 들어 하나의 박스를 쪼개어 "메뉴명(rat:0.7)"과 "가격(rat:0.3)"을 만들었다면, 메뉴명에 "horizontal"을 주었으면 가격에도 반드시 "horizontal"을 주어야 한다.
            
            - 같은 원본 박스에서 파생된 객체들끼리 하나는 "vertical", 하나는 "horizontal"로 방향이 엇갈리게 배정하는 기괴한 짓을 절대 하지 마라. 화면 레이아웃이 완전히 박살 난다.

          [다중 세트 병합 쪼개기 (UI 보호용 예외 규칙)]: 1개의 상자 안에 '메뉴+가격' 세트가 2개 이상 연속으로 뭉쳐 있는 경우 (예: 메뉴A + 가격A + 메뉴B + 가격B), 이를 메뉴와 가격으로 일일이 4등분하면 UI 오버레이가 겹쳐서 붕괴돼.

              이런 경우에는 절대 가격을 독립된 "price" 타입으로 쪼개지 마!

              대신 **[메뉴A+가격A]**를 한 덩어리로 묶어서 1개의 "menu_name" 객체로, **[메뉴B+가격B]**를 또 다른 1개의 "menu_name" 객체로 만들어. (총 2개의 객체로만 분할해)

              방향(dir)은 무조건 **"vertical"**을 주고 비율(rat)은 균등하게 배분해.

              가격 숫자와 화폐 단위는 ko 번역문 안에 자연스럽게 흡수시켜라. (예: ko: "걸쭉한 백탕 라멘 1080엔", pr: null)

              단, nm (정규화)에서는 가격 숫자와 기호를 완벽히 지우고 요리명만 남겨라!

        C. 삭제 (Delete/Drop): 의미 없는 단일 자음/모음/알파벳, 고립된 특수기호 덩어리 등 명백한 노이즈일 때 삭제해.

          - [단일 글자 삭제 방어]: 1글자짜리 텍스트 블록("새", "우" 등)을 마주하더라도 "노이즈"라고 섣불리 판단하여 삭제하지 마. 무조건 해당 박스의 위/아래(Y좌표)에 인접한 다른 박스가 있는지부터 확인하고, '세로쓰기 메뉴'의 일부라면 'A. 합치기' 프로세스로 넘겨. 주변에 아무것도 없이 완전히 고립된 경우에만 삭제를 진행해.

          - [숫자 판별 및 절대 삭제 금지 규칙]: 숫자만 적힌 블록은 아래 기준을 통해 '가격'인지 엄격히 구별해.

            ① [가격으로 판단 / 삭제 금지]: 100, 1000 단위로 끝나는 숫자, 쉼표(,)나 소수점(.)이 포함된 숫자, 화폐 기호가 결합된 숫자.

            ② [노이즈로 판단 / 삭제 진행]: 다른 메뉴와 합칠 수도 없고 완전히 고립된 단순 숫자(페이지 번호 오인식 등), 가격으로 보기 힘든 불규칙 숫자(전화번호, 날짜 등).

            ③ 부가 정보 보호: 서술형 문장이나 매장 안내는 특수 기호가 있어도 절대 삭제 금지.


          D. 1:1 유지 (Normal): 1개 블록 = 1개 정보인 경우, `box`에 원본 ID 1개만 넣고 `dir`, `rat`는 무조건  `null`로 설정해. dir은 무조건 null로! 



      3. 식별자 부여 및 아이템 타입(type) 분류

      - 보정/살아남은 각 항목에 "result_001", "result_002" 형태로 순차적인 고유 `id`를 부여해.

      - `type`을 텍스트의 형태가 아닌 '실제 역할'을 기준으로 다음 중 하나로 명확히 분류해:

        * "menu_name": 고객이 실제로 비용을 지불하고 '주문(Order)' 및 '취식'할 수 있는 개별 요리, 사이드 메뉴, 음료수, 그리고 각종 추가 토핑(파 추가, 계란 추가, 사리 등)명.
          
          -"단독 요리가 아니더라도, 비용이 별도로 책정된 사리 추가, 사이즈업(대짜, 곱빼기 등), 토핑 추가 등은 무조건 삭제하지 말고 "menu_name"으로 살려서 분류해라. (nm에서 수식어를 지우라고 했지, 객체 자체를 삭제하라는 뜻이 절대 아니다!)"

        * "price": 메뉴의 가격 정보. (화폐 기호가 없는 단순 숫자 형태라도 레이아웃과 문맥상 가격을 의미하면 포함)

        * "description": 개별 메뉴의 부가 정보(재료, 원산지, 맛 설명)뿐만 아니라, 식당 이용 안내문, 그리고 "메뉴판의 구조를 나누는 분류 기준이나 제목(카테고리명)" 등 "menu_name"과 "price"에 속하지 않는 모든 텍스트.
                      - 포르투갈 메뉴판에서 요리사의 영혼(Alma do Cozinheiro)은 무조건 description으로 분류해!! 

        * [설명문 오판 절대 금지]: 문장의 길이가 길거나, 쉼표(,)로 여러 식재료가 나열되어 있거나, 요리의 조리법/제공 형태를 길게 서술하는 문장(예: ~served with, ~topped with)은 무조건 "description"으로 분류해. 임의로 핵심 단어를 뽑아내어 "menu_name"으로 둔갑시키지 마.

        * [언어 불일치 강제 분류]: raw 텍스트의 주된 작성 언어가 {{departure_lang}}과 명백히 다른 경우(예: 관광객용 병기 번역 등), 해당 텍스트의 type은 원칙적으로 "description"으로 분류해.

        * [언어 불일치 예외 조건 (절대 규칙)]: 단, 언어가 다르더라도 그 텍스트가 영문/로마자로 표기된 **주류, 음료 브랜드, 글로벌 통용 명칭(예: Coca Cola, Sprite, Beer, Wine 등)**이거나, 문맥상 명백히 '독립적으로 주문 가능한 항목'일 경우에는 예외적으로 "menu_name"을 부여해.

        * [사이즈 및 가격 분류 (절대 규칙)]: 텍스트에 요리 이름 없이 "大, 小, M, L, Regular" 같은 사이즈 표기와 가격(숫자)만 결합되어 있다면, 절대 menu_name이나 description으로 분류하지 마. 무조건 price로 분류해.

        * [다국어 병기 강제 분리]: 현지어(예: 중국어) 메뉴명 주변에 영어 등 글로벌 언어로 된 번역본이 적혀 있을 경우, 절대 두 박스를 합치거나 영어를 menu_name으로 분류하지 마. 현지어는 menu_name으로 두고, 영어 번역 박스는 무조건 description으로 분류해. (이렇게 해야 UI 화면에서 영어 글씨 위에도 누락 없이 번역 오버레이를 씌울 수 있어.)

        * [추상적 문구 및 슬로건 분류 절대 규칙]: "요리사의 영혼", "우리의 철학" 등과 같이 고객이 실제로 주문하고 먹을 수 있는 물리적인 '음식'이 아닌 감성적인 문구, 식당의 슬로건, 또는 카테고리를 꾸며주는 부제목은 절대 menu_name으로 착각하지 마라. 묻지도 따지지도 말고 무조건 **"description"**으로 분류해라.
        
        * [옵션 나열형 메뉴 보호]: 텍스트가 길거나 하이픈(-), 슬래시(/) 등으로 연결되어 있더라도, 그것이 요리의 재료 설명이 아니라 '선택 가능한 요리/샌드위치/맛의 종류들(예: 믹스 - 채식 - 치킨 - 참치)'을 나열한 것이라면 절대 description으로 빼지 말고 menu_name으로 분류해라.

        * [가격 강제 분류 절대 규칙]: 텍스트 내에 숫자와 함께 명백한 **화폐 기호(€, $, ¥, 元, 원 등)**가 포함되어 있다면, 다른 문맥을 따질 필요 없이 묻지도 따지지도 말고 무조건 type을 "price"로 고정해라. 절대 "description"으로 오판하지 마!

        * [슬로건 및 홍보 문구 철저 배제]: 전 세계 어떤 언어의 메뉴판이든, 텍스트가 식당의 서술형 광고 문구, 환영 인사, 또는 먹는 방법을 설명하는 문장형 텍스트(예: "정통 방식을 고수합니다", "~를 즐겨보세요", "~하는 특별한 방법" 등)인 경우, 그 문장 안에 요리나 식재료 이름이 섞여 있더라도 절대 menu_name으로 분류하지 마라. 이는 묻지도 따지지도 말고 무조건 **"description"**으로 처리해라.




        4. 언어 판별 필터 및 번역 (ko 생성)
        - raw를 한국어로 번역해         

        - [특수기호 및 맛 표현 절대 보존]: 원본(`raw`)에 포함된 특수 기호(※, ●, ★, ▶, 괄호 등), 무의미한 문자, 숫자, 형용사, 맛 표현은 절대 누락하지 말고, 번역된 문장(`ko`) 안의 원래 위치에 그대로 살려서 출력해.
       
         - 문화적 차이로 직역이 어색한 요리명은 직관적인 단어로 의역/음역하고(예: '親子丼' -> '오야코동'), 식재료(양배추, 적배추 등)는 명확히 구분해 번역해.

         -[철칙 1. 글로벌 고유 명사 보호]: '오야코동(親子丼)', '스시', '똠얌꿍', '팟타이' 등 한국인에게 이미 원어 발음 그대로 널리 알려진 유명한 요리명은 억지로 재료명으로 풀어서 번역하거나 어색하게 직역(예: 부모 자식 덮밥)하지 마라. 대중적으로 통용되는 고유 명칭(발음) 그대로 살려라.

          [철칙 2. 낯선 현지 식재료 완벽 의역 (단순 음역 금지)]: 반면, 한국인에게 낯선 현지 고유의 식재료나 조리 방식이 나열된 경우, 발음만 기계적으로 옮겨 적는 '단순 음역'을 절대 금지한다. (예: 베트남어 Nạm -> 남(X),. 이런 경우에는 한국인이 직관적으로 100% 이해할 수 있도록 가장 대중적이고 친숙한 식재료 명칭 및 조리법(예: 소고기 양지, 고기완자, 고기 볶음)으로 완벽하게 풀어서 의역해라.

          [철칙 3. 끔찍한 혼종 번역 절대 금지 (1과 2 섞어 쓰기 금지)]: 하나의 요리명이나 식재료 덩어리를 번역할 때, 철칙 1(발음 유지)과 철칙 2(뜻풀이)를 기괴하게 반반씩 섞어 쓰지 마라.

          [철칙 4. 특수기호 및 맛 표현 절대 보존]: 원본(raw)에 포함된 특수 기호(※, ●, ★, ▶, 괄호 등), 무의미한 문자, 숫자, 형용사, 맛 표현(매운, 달콤한 등)은 절대 누락하지 말고, 번역된 문장(ko) 안의 원래 위치에 그대로 살려서 출력해라.

          [상위 카테고리 문맥 종속 번역]: 메뉴명이 짧은 단일 단어나 형용사(예: '별 모양의', '이혼한', '전통적인', '매운' 등)로만 고립되어 적혀 있을 때, 그 단어 하나만 보고 기괴하게 직역하지 마라.
            반드시 해당 메뉴의 주변이나 바로 위에 위치한 '상위 카테고리 명칭(예: 계란 요리, 소고기 부위, 면류, 수프 등)'의 문맥을 하나로 결합하여 해석해라. 해당 단어가 그 카테고리 내에서 실제로 어떤 '특정 식재료'나 '조리 방식(예: 계란 프라이, 스크램블, 웰던 등)'을 의미하는지 현지 식문화 맥락에서 파악한 후, 한국인이 직관적으로 이해할 수 있는 친숙하고 자연스러운 요리명으로 완벽하게 의역해라.

        5. 가격 숫자 추출 (pr 임시 생성)

        - type이 "menu_name" 이거나 "description"일 경우에는 무조건 pr을 null로 처리해.
        - type이 "price"인 경우, 절대 수학적 환율 계산을 하지 마.
        - 대신 raw에서 화폐 기호($, 円, 원, €, ₽, P 등)나 불필요한 문자를 모두 제거하고, 오직 순수한 숫자(소수점 포함)만 추출하여 pr에 문자열로 기입해. (예: "$ 35" -> "35", "14.99" -> "14.99")
        - type이 "price"인데 원본에 사이즈 표기(大, 小 등)가 섞여 있다면, 사이즈 문자는 지우고 오직 숫자만 추출해 pr에 넣어. (예: "大 40" -> "40")

        - [절대 규칙] 전 세계 공통 OCR 오인식 문맥 보정:
          화폐 기호나 단위 문자가 숫자로 오인식되어 가격이 비정상적으로 부풀려지는 현상이 잦아. 주변 메뉴의 평균 가격대 문맥을 철저히 비교하여, 특정 항목의 숫자가 문맥에 맞지 않게 높거나 불규칙하다면 반드시 오인식된 맨 끝자리(또는 맨 앞자리)를 과감히 잘라내거나 소수점을 찍어서 합리적으로 보정해라!

          * 패턴 1 (10배 이상 폭등): 주변 가격대가 6~20인데 혼자 1670, 268인 경우 -> 끝자리 삭제 (예: "1670" -> "16", "268" -> "26")
          * 패턴 2 (반복 숫자): 특정 메뉴만 '88', '66' 처럼 앞 숫자가 비정상적으로 반복되는 경우 -> 중복 글자 1개 삭제 (예: "88" -> "8")
          * 패턴 3 (전 세계 화폐 기호의 숫자 오인식 - 만능 방어 룰):
            입력된 메뉴판의 언어(departure_lang)에서 주로 사용하는 화폐 기호(예: $, €, ¥, 元, 원, ₫, ฿, Rp, ₽, P, AED 등)가 무엇인지 먼저 파악해라. 화폐 기호는 보통 숫자의 맨 앞이나 맨 뒤에 붙는데, OCR이 이 기호의 모양을 숫자(예: 0, 2, 5, 6, 8 등)로 잘못 읽어 가격 텍스트에 원치 않는 숫자가 추가되는 경우가 모든 언어권에서 공통으로 발생한다.
            - [맨 뒤 기호 오인식 예시]: 러시아 "75 P" -> "752" / 유로 "15 €" -> "156" / 베트남 "25000 ₫" -> "250000"
            - [맨 앞 기호 오인식 예시]: 미국 "$ 20" -> "520" / 태국 "฿ 80" -> "880"
            - [행동 지침]: 따라서, 주변 메뉴들의 평균 가격대와 비교했을 때 특정 메뉴의 가격이 비상식적으로 자릿수가 튀거나, 맨 앞이나 맨 뒤에 불규칙한 숫자가 하나 더 붙어 있다면 이는 '해당 국가의 화폐 기호가 숫자로 오인식되어 달라붙은 것'으로 절대 간주해라! 주저하지 말고 맨 앞이나 맨 뒤에 붙은 오인식 숫자 1자리를 과감히 잘라내어 문맥에 맞는 합리적인 가격으로 완벽하게 복원해라.

        6. 데이터 검증 및 정규화 (nm 생성)

          -[절대 규칙] "ko"가 null이면 무조건 "nm"도 null로 처리해

          - type이 "menu_name"일 때만 nm을 무조건 생성해

          - type이 "price" 나 "description"일 경우 `nm`을 반드시 null으로 처리해.

          - `nm`은 DB 검색용이야 무조건 불필요한 수식어는 뺀 상태에서 메뉴 이름을 검색할 수 있도록 반환해.

          - [절대 규칙] 핵심 요리명 추출 (껍데기 완벽 제거): `nm`은 DB 매핑을 위한 뼈대 데이터야. `ko` 텍스트에서 요리의 '본질'이 아닌 모든 수식어를 100% 쳐내.
              * [제거 대상]: 형용사/맛표현(매운, 맛있는, 수제, 옛날 등), 기호/숫자/단위, 음식의 크기 및 분량(소/중/대, 미니, 대왕, 점보, 곱빼기, 반 마리 등), 제공 형태(세트, 정식, 모둠, ~추가, 사리 등).
              * [보존 대상]: 오직 '무엇을 먹는가'를 나타내는 핵심 식재료와 명사형 요리명만 남겨. 외국의 경우 핵심 식재료에 대한 명칭을 삭제하면 안돼. (일본의 경우: 소유, 돈코츠..)
              * [적용 예시]: 
                - "맛있는 흑돼지 군만두 세트" -> "흑돼지군만두"
                - "면 곱빼기" -> "면"
                - "미니 우동" -> "우동"
                - "당면 사리 추가" -> "당면"

          - [띄어쓰기 절대 규칙]: 정규화된 단어가 2개 이상 나오더라도 **띄어쓰기를 다 없앤 상태**로 반환해. (예: "새우 튀김 덮밥" -> "새우튀김덮밥")

          - nm에는 한국어로 된 메뉴명만 들어갈 수 있어 !!!



        ---

        [절대 규칙]

        1. 누락 금지: 삭제(Delete) 조건에 해당하지 않는 한, 입력받은 모든 데이터를 단 하나도 빠짐없이 원래 순서(위치 기반)대로 반환해.

        2. 출력 포맷 (엄격 준수): 최상단에 st, err_c, err_m를 포함하고, `items` 배열 안에는 아래의 JSON 구조를 완벽하게 유지해.


        [출력 JSON 형식 예시]
        "dir": "horizontal", // rat가 존재하므로 절대 null 금지

        {{

          "st": "success",

          "err_c": null,

          "err_m": null,
          
          "cr": "KRW",

          "items": [

            {{

              "id": "result_001",

              "c": {{

                "type": "menu_name",

                "raw": "★海老 天丼",

                "ko": "★새우 튀김 덮밥",

                "pr": null,

                "nm": "새우튀김덮밥"

              }},

              "l": {{

                "box": ["box_001", "box_002"],

                "dir": null,

                "rat": null

              }}

            }},

            {{

              "id": "result_002",

              "c": {{

                "type": "price",

                "raw": "800",

                "ko": "7,200원",

                "pr": "7200",

                "nm": null

              }},

              "l": {{

                "box": ["box_003"],

                "dir": null,

                "rat": null

              }}

            }}

          ]

        }}
    """

    
  actual_data_str = json.dumps(raw_menus, ensure_ascii=False, separators=(',', ':'))
  final_prompt = f"{system_prompt}\n\nInput:\n{actual_data_str}"

  try:
      response = model.generate_content(final_prompt)
      llm_result = json.loads(response.text)

      frontend_items = []
      for item in llm_result.get("items", []):
          c = item.get("c", {})
          l = item.get("l", {})
          
          frontend_items.append({
              "item_id": item.get("id"),
              "content": {
                  "item_type": c.get("type"),
                  "raw_text": c.get("raw"),
                  "translated_text": c.get("ko"),
                  "converted_price": c.get("pr"),
                  "normalized_text": c.get("nm")
              },
              "layout": {
                  "source_box_ids": l.get("box", []),
                  "direction": l.get("dir"),
                  "ratio": l.get("rat")
              }
          })

      final_response = {
          "status": llm_result.get("st", "error"),
          "error_code": llm_result.get("err_c"),
          "error_message": llm_result.get("err_m"),
          "currency": llm_result.get("cr", "KRW"),
          "analyzed_menu_items": frontend_items
      }

      return final_response
      
  except Exception as e:
      print(f"LLM 처리 중 에러 발생: {e}")
      return {
          "status": "error",
          "error_code": "LLM_ERROR",
          "error_message": str(e),
          "analyzed_menu_items": []
      }